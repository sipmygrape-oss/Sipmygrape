import streamlit as st import pandas as pd import json from groq import Groq # Titre st.title("🍷 SipMyGrape : Ton Sommelier IA") # Charger les données (ton fichier JSON) @st.cache_data def load_data(): with open('dataset.json', 'r', encoding='utf-8') as f: return json.load(f) data = load_data() # Convertir en DataFrame pour faciliter la lecture df = pd.DataFrame(data) # Zone de chat user_input = st.text_input("Pose ta question sur les vins :") if user_input: # Ici on utilise la clé secrète (on la configurera dans Streamlit) client = Groq(api_key=st.secrets["GROQ_API_KEY"]) # Résumé des posts pour l'IA contexte = df.to_string() completion = client.chat.completions.create( model="llama-3.3-70b-versatile", messages=[{"role": "user", "content": f"Voici mes posts Instagram : {contexte}. Réponds à : {user_input}"}] ) st.write(completion.choices[0].message.content)